<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:label="TopperThali"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Main Launcher Activity -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- New Add Student Screen -->
        <activity android:name=".ui.students.AddStudentNewActivity"/>

        <activity android:name=".ui.students.StudentListActivity"
            android:label="Student Directory"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".ui.students.AddStudentActivity"
            android:label="Add Student"
            android:parentActivityName=".ui.students.StudentListActivity"/>

    </application>

</manifest>
